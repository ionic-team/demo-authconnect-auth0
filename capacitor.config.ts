import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.demo.ac.auth0',
  appName: 'AuthConnect Auth0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
